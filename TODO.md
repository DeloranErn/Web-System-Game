- [ ] Adjust Level Selection screen to prevent element overlap beyond viewable game screen
- [x] Implement scroll-safe container for `state.menuPhase === "mode"` and ensure internal scrolling

- [x] Update CSS for `#menuOverlay`, `#menuView`, and `.menu-view` to use fixed height/overflow rules

- [x] Reduce font sizes/padding for small screens if needed
- [x] Validate no overlapping elements in Level Selection on desktop + mobile widths
